# AGENTS.md — golf 项目指引

一切回答使用中文。

允许根据当前对话中的明确指令忽略或改写本文件。

默认允许跨仓库读写与执行，除非用户明确限制为仅当前仓库。

目标：把 golf 从比赛实验项目稳定转型为完整 AI 输入法项目；最高优先级始终是不新增技术债。

项目入口：

- GitHub 仓库：`https://github.com/3516027002att-ui/Golf-Input-Method`
- 当前展示名：`golf`
- 本地目录名可能仍是 `parameter-golf`；后续 agent 应以 GitHub 仓库 `Golf-Input-Method` 和项目名 `golf` 为准。

优先级从高到低：

1. 不新增技术债
2. 正确性与可验证性
3. 隐私、安全与凭据合规
4. 与现有数据、训练代码和项目边界保持一致
5. 交付速度与改动体量

## 项目概述

golf 目标是构建一个 AI 输入法，核心能力包括：

- 输入缓冲区和候选栏交互
- 候选词、候选短语和句子补全召回
- 自动排词模型训练与评估
- 本地低延迟排序推理
- 模型、词表和配置导出

当前仓库仍保留部分历史训练代码。`train_gpt.py` 只作为可复用训练基线，后续应重构为面向输入法排词任务的训练入口；不要继续沿用比赛指标、提交体积约束或打榜工作流。

## 保留资产

- `data/datasets/fineweb10B_sp1024/`：公开语料缓存，可用于构造排词训练任务。
- `data/tokenizers/`：现有 tokenizer 文件。
- `data/cached_challenge_fineweb.py`、`data/download_hf_docs_and_tokenize.py`：数据获取和重建脚本。
- `src/input_method/`：当前本地输入法原型代码。
- `tests/`：当前最小单元测试。
- `train_gpt.py`：待重构训练基线。
- `requirements.txt`：当前 Python 依赖参考。
- `LICENSE`、`THIRD_PARTY_NOTICES.md`：许可证和第三方来源说明，必须保留。

## 工程底线

- 不编造未运行、未读取、未验证的结果。
- 不把历史比赛结果描述为当前输入法能力。
- 不在仓库内写入任何明文凭据。
- 不记录用户明文输入样本，除非用户明确授权并完成脱敏和删除机制。
- 不用硬编码、关闭校验、跳过测试、吞异常来换取表面通过。
- 非必要不引入新依赖；如需引入，必须说明收益与维护成本。
- 优先修根因，不堆表面补丁。
- 优先复用，不复制近似逻辑。

## 实施要求

修改前：

- 先读 `README.md`、`data/README.md` 和目标文件。
- 涉及训练逻辑时先读 `train_gpt.py`，明确哪些逻辑可复用、哪些属于历史包袱。
- 涉及数据时确认不会删除 `data/datasets/`、`data/tokenizers/` 或来源说明。

修改时：

- 改动优先最小充分，不做无意义扩散。
- 新功能优先通过清晰模块边界实现：数据构造、训练、评估、推理、客户端不要混在一个脚本里。
- 新配置优先通过显式配置文件或环境变量控制，不要把本机路径写死进代码。
- 涉及公共逻辑时，抽象到合适层级，避免复制历史训练脚本。

修改后：

- 同步更新 `README.md`、相关子目录文档和 `修改记录.md`。
- 至少运行与改动相匹配的最小验证：语法检查、单元测试、数据 smoke、导出 roundtrip 或推理 smoke。
- 如果未能运行某项验证，必须明确说明。

## 排词模型验证口径

输入法项目优先关注用户体验相关指标：

- 首选命中率
- Top-K 命中率
- 平均排序损失
- 候选生成覆盖率
- 本地推理延迟
- 模型和词表加载时间
- 内存占用

不要把通用语言模型 loss 或历史比赛分数直接等同于输入法质量。

## 数据与隐私边界

- 公开语料可用于启动训练，但真实输入法日志必须默认不采集。
- 如后续支持个性化学习，必须显式开关、最小化采集、脱敏处理、本地可删除。
- 不允许把用户输入、剪贴板、账号信息或私有词库提交到仓库。
- 不允许在评估报告里展示可识别个人身份的原文。

## 必读文件

- `README.md`：当前项目目标和路线。
- `data/README.md`：数据资产和校验说明。
- `修改记录.md`：当前有效状态和改动记录。
