# 修改记录

## 2026-06-06 Context Reranker V2 训练闭环

### 当前有效状态

- 新主线固定为 `training/context_reranker_v2.py`，第一轮训练只使用 `train_new_corpus.jsonl`、`val_new_corpus_v2.jsonl`、`test_new_corpus_v2.jsonl`。
- 旧 `train.jsonl` 不作为第一轮主训练集；本轮没有改 UI，也没有把 `plan1_reranker.py` 重新设为新主线。
- 本地负责 audit、tiny smoke、sanity 和离线 predict 验收；Colab notebook 负责正式 GPU 训练、完整 eval 和 checkpoint 导出。

### 本次修改

- `scripts/run_context_v2_local_audit.ps1` 支持可覆盖 `DataRoot`，默认仍指向 Google Drive 新语料目录。
- 新增 `scripts/run_context_v2_local_smoke.bat` / `.ps1`：从新语料抽小样本，跑 audit、online tiny train、random-label tiny train、online/no-context/shuffle/random-label eval 和离线 predict，并写出 `summary.json`。
- `notebooks/colab_context_reranker_v2.ipynb` 改为可复现的 Colab 主入口：mount Drive、clone 当前分支、安装 `requirements-train.txt`、复制新语料、先 audit/smoke/sanity，再按开关执行正式训练并保存到 Drive。
- 新增 `docs/context_reranker_v2_training_workflow.md`，记录本地 gate、Colab 输出目录、正式训练开关和本地离线验收边界。

### 最终验证记录

- `python -m unittest tests/test_context_reranker_v2.py`：通过，10 tests OK。
- `scripts\run_context_v2_local_audit.bat`：通过，生成 `reports\context_v2_new_corpus_audit.md`。
- `scripts\run_context_v2_local_smoke.bat`：链路通过，生成 `reports\context_v2_local_smoke\<timestamp>\summary.json`、tiny checkpoint、eval JSON 和 `predict_xuexiao.json`。
- smoke 红线未被静默吞掉：tiny online top1 为 0.0417，no-context top1 为 0，shuffle top1 为 0.1667，random-label top1 为 0.125；`predict` 样例中“学校”排第 3，不能宣称模型质量可用。
- Colab CLI 已做过辅助 smoke；正式可复现入口以 `notebooks/colab_context_reranker_v2.ipynb` 为准。

### 结论

完成训练前工程闭环。当前 tiny smoke 只证明代码、依赖、路径、checkpoint、eval 和离线 predict 链路可跑；正式模型效果仍必须等 Colab 完整训练和本地真实输入场景验收后再判断。

---

## 2026-06-02 Plan1 Colab Pro warm-start 标签空间迁移与本地监督

### 当前有效状态

- `training/plan1_reranker.py train` 已支持 `--checkpoint-label-space-policy strict|expand-compatible`。
- 默认 `strict` 保持旧行为：checkpoint 标签空间必须与当前训练数据完全一致。
- 显式 `expand-compatible` 时，训练 warm-start 会按 label 名称迁移 `match_embedding`、`source_embedding`、`domain_embedding`；新 label 从旧 `unknown` 行初始化，并生成 `label_space_migration.json`。
- 推理侧 `Plan1InferenceSession.load()` 仍保持严格加载，不做隐式迁移。
- 已新增 `scripts/watch_plan1_colab.py`，用于本地 agent 从 Google Drive 同步目录只读监督 Colab run。

### 本次修改

- 新增 checkpoint 标签空间扩展迁移函数，要求 `encoder_name`、`max_length`、上下文窗口和 `numeric_feature_names` 完全兼容；旧 label 必须是新 label 的子集，否则 fail-closed。
- 迁移审计记录旧/新 label map、复制 label、新增 label、从 `unknown` 初始化的 label、随机保留 label 和 checkpoint 路径。
- `dry_run_manifest.json` / `training_config.json` 会记录 `checkpoint_label_space_policy`。
- `scripts/watch_plan1_colab.py` 读取 `heartbeat.json`、`metrics.jsonl`、`final_test_metrics.json`、`cleaning_feedback/*.json`、`train.log` 和 `last_state.pt`，输出 stale、NaN/Inf、验证退化、弱数据切片等预警。
- `README.md` 和 `data/README.md` 补充 warm-start Colab 命令、本地 watcher 命令和迁移边界。

### 最终验证记录

- `python -m py_compile training\plan1_reranker.py scripts\watch_plan1_colab.py tests\test_reranker.py`：通过。
- `python training\plan1_reranker.py train --help`：通过，出现 `--checkpoint-label-space-policy {strict,expand-compatible}`。
- `python -m pytest tests\test_reranker.py -q`：通过，8 passed。
- `python training\plan1_reranker.py train --dry-run --device cpu --colab-pro-mode --checkpoint-label-space-policy expand-compatible --init-from-checkpoint "C:\training data\golf-ime-data\models\plan1-reranker\best" --train-path "C:\training data\golf-ime-data\processed\plan1_ranking_colab_ready_20260602_train.jsonl" --val-path "C:\training data\golf-ime-data\processed\plan1_ranking_colab_ready_20260602_val.jsonl" --test-path "C:\training data\golf-ime-data\processed\plan1_ranking_colab_ready_20260602_test.jsonl" --output-dir "C:\training data\golf-ime-data\models\codex_warmstart_label_migration_finalcheck_20260602" --per-device-batch 3 --grad-accum 8 --max-epochs 1 --min-epochs 1 --patience 1`：通过，train/val/test 为 930000/55000/55000，生成 `label_space_migration.json`。
- `label_space_migration.json` 审计确认：新增 `domain_to_id` label 为 `community_qa`、`encyclopedia_qa`、`news`，均从旧 `unknown` 行初始化，`kept_random_initialization` 为空。
- `--checkpoint-label-space-policy strict` 搭配旧 best checkpoint 与 full data dry-run：按预期拒绝，报错为 `domain_to_id` 标签空间不一致。
- `python scripts\watch_plan1_colab.py --run-dir "C:\training data\golf-ime-data\models\codex_resume_runtime_guard_finalcheck_20260602" --once`：通过，能读取 heartbeat、last_state 和 global_step，并对缺失 `train.log` 输出预警。
- 数据复核：`plan1_ranking_colab_ready_20260602` 三 split 行数为 930000 / 55000 / 55000，SHA256 与 manifest 一致，`document_leakage_count=0`。
- 断点恢复 smoke：短 runtime guard 生成 `last_state.pt` 和 `heartbeat.json`，真实 `--auto-resume` 可从已保存的 epoch/batch/global_step 连续前进。
- import smoke：`InputMethodConfig`、`InputMethodEngine`、`PinyinCandidateGenerator`、`FrequencyReranker`、`GuiEditor`、`GuiCandidateWindow` 均可按当前实际模块路径导入。
- `python -m pytest tests/ -q`：通过，66 passed。
- `python scripts\evaluate_candidates.py`：通过，总体 Top-1 94.1%，Top-3/Top-5/召回覆盖率 100.0%。
- `python scripts\benchmark_latency.py`：通过，P99 远低于 5ms，处于 0.1ms 量级。
- `git diff --check`：通过，仅有当前工作区既有 LF/CRLF warning。

### 结论

完成。Colab Pro warm-start 的标签空间阻塞已解除，本地 agent 可通过 Drive 同步目录监督训练；该数据仍含 `unknown` / `research_only` license bucket，仅用于内部研究训练，不可公开发布或再分发。

---

## 2026-06-02 Plan1 训练反馈清洗闭环

### 当前有效状态

- `training/plan1_reranker.py train` 现在会记录训练数据版本、清洗规则版本和上游数据 manifest 摘要，用于把训练结果反向关联到具体清洗数据包。
- 每轮验证和最终测试默认会在模型输出目录 `cleaning_feedback\` 下生成脱敏反馈报告，报告用于指导下一轮 `audit-data` / `clean-data` 清洗迭代。
- 反馈报告不会写入上下文原文、target 文本或候选文本，只保留样本哈希 ID、来源/许可/领域/长度/候选数量等切片信息和高损失排序信号。
- 本次未启动正式训练，未声明模型质量提升，也未覆盖现有训练数据文件。

### 本次修改

- `training/plan1_reranker.py` 新增训练数据 profile：自动从 train/val/test 路径推断 `data_version`，并查找同前缀的 `_data_manifest.json`、`_audit.md` 和 `_sha256.json`。
- `data_manifest.json` / `training_config.json` 新增 `data_version`、`cleaning_rules_version`、`data_profile` 和 `cleaning_feedback_schema_version`。
- 验证/测试评估新增清洗反馈聚合：按 `sample_source`、`license_bucket`、`domain`、`source_doc_prefix`、候选数量桶、输入长度桶和 target 初始位置桶统计 loss / Top-1 / Top-3 / Top-5 / MRR / NDCG@10。
- 新增脱敏高损失样本列表，只保留 `sample_id`、`source_doc_key_hash`、切片字段、loss、rank 和预测位置等字段。
- `train` 子命令新增：
  - `--no-cleaning-feedback`
  - `--cleaning-feedback-top-n`
  - `--cleaning-feedback-min-slice-samples`
- `README.md` 和 `data/README.md` 补充说明：清洗规则仍在数据管线中，训练侧只输出下一轮清洗所需的反馈信号。

### 最终验证记录

- `python -m py_compile training\plan1_reranker.py`：通过。
- `python training\plan1_reranker.py train --help`：通过，新增清洗反馈参数已出现。
- `python training\plan1_reranker.py build-data --help`：通过。
- 最小反馈函数 smoke：通过，`plan1_cleaning_feedback_v1` 报告结构、切片指标和高损失样本结构可生成。
- import smoke：`InputMethodConfig`、`InputMethodEngine`、`PinyinCandidateGenerator`、`FrequencyReranker`、`GuiEditor`、`GuiCandidateWindow` 均可导入。
- `python -m pytest tests/ -q`：65 passed。
- `python scripts\evaluate_candidates.py`：通过，总体 Top-1 94.1%，Top-3/Top-5/召回覆盖率 100.0%。
- `python scripts\benchmark_latency.py`：通过，P99 约 0.07ms。
- `git diff --check`：通过，仅有当前工作区既有 LF/CRLF warning。

### 结论

完成。训练文件已经具备“训练结果反向指导数据清洗”的最小闭环；后续应通过真实训练产生的 `cleaning_feedback\*.json` 观察差切片，再回到 `audit-data` / `clean-data` 调整数据。

---

## 2026-06-02 Plan1 Colab 训练数据收口 — 审计、清洗、补全与打包

### 当前有效状态

- 已从原有 `plan1_ranking_multi_1m_90_5_5` 数据完成完整审计、自适应清洗、按 `source_doc_key` 稳定哈希重切分、补全与打包。
- 最终输出前缀 `plan1_ranking_colab_ready_20260602`，所有产物放置在仓库外 `C:\training data\golf-ime-data\` 下。
- 最终数据包可直接上传 Colab 开始训练，无需再执行任何数据清洗或切分。

### 本次修改

- `training/plan1_reranker.py` 新增三个子命令：
  - `audit-data`：审计指定前缀的 train/val/test 数据，生成 audit markdown（样本数、坏样本统计、候选分布、来源分布、许可证分布、oracle Top-K、文档泄漏检查）。
  - `clean-data`：清洗输入数据、修复可恢复字段、从词库补全单候选样本、按 `source_doc_key` 稳定哈希重切分 90/5/5、从语料补全不足 split、生成 manifest / audit / sha256。
  - `package-data`：将最终 split、manifest、audit、sha256 复制到 exports 目录，生成 zip。
- 新增数据清洗库函数：`normalize_source_doc_key`、`clean_one_sample`、`_supplement_candidates`、`_generate_supplement_from_corpus_v2`、`_write_split_files_v2`、`compute_sha256` 等。
- 未新增分散脚本，未覆盖原有 `plan1_ranking_*.jsonl` 或 `plan1_ranking_multi_1m_90_5_5_*` 文件。

### 最终数据统计

| Split | 原始样本 | 清洗保留 | 删除 | 补全 | 最终数量 | 硬下限 | 软目标 |
|-------|---------|---------|------|------|---------|--------|--------|
| train | 926,636 | 762,822 | 163,814 | 167,178 | **930,000** | 900,000 ✅ | 930,000 ✅ |
| val   | 54,392  | 44,047  | 10,345 | 10,953 | **55,000**  | 50,000 ✅  | 55,000 ✅  |
| test  | 44,258  | 34,421  | 9,837  | 20,579 | **55,000**  | 50,000 ✅  | 55,000 ✅  |
| total | 1,025,286 | 841,290 | 183,996 | 198,710 | **1,040,000** | — | — |

### 删除与修复明细

- **删除 183,996 条**：
  - 183,985 条因 candidates 去重后不足 2 个且无法从词库补全
  - 11 条因 composing 为空
- **修复 1,124 条**：
  - 437 条从词库补全候选列表
  - 437 条重建 candidate_features
  - 250 条修正 target_index

### 补全来源

- 从 `zhwiki_corpus.jsonl` 生成 198,710 条补充样本（train: 167,178, val: 10,953, test: 20,579）
- 补充样本全部通过同一清洗规则并按 `source_doc_key` 稳定哈希进入正确 split

### 数据质量

- 文档泄漏：**0**（同一 `source_doc_key` 不跨 split）
- Oracle Top-1: train 9.9%, val 9.5%, test 9.4%
- Oracle Top-10: 全部 100%
- License: attribution 40.9%, research_only 1.3%, unknown 57.8%
- 许可证说明：含 `unknown` 或 `research_only` 的数据仅限内部研究训练，不可公开发布或再分发

### 交付物

- `C:\training data\golf-ime-data\processed\plan1_ranking_colab_ready_20260602_train.jsonl` (930,000 条)
- `C:\training data\golf-ime-data\processed\plan1_ranking_colab_ready_20260602_val.jsonl` (55,000 条)
- `C:\training data\golf-ime-data\processed\plan1_ranking_colab_ready_20260602_test.jsonl` (55,000 条)
- `C:\training data\golf-ime-data\manifests\plan1_ranking_colab_ready_20260602_data_manifest.json`
- `C:\training data\golf-ime-data\manifests\plan1_ranking_colab_ready_20260602_audit.md`
- `C:\training data\golf-ime-data\manifests\plan1_ranking_colab_ready_20260602_sha256.json`
- `C:\training data\golf-ime-data\exports\plan1_ranking_colab_ready_20260602\`（含全部文件）
- `C:\training data\golf-ime-data\exports\plan1_ranking_colab_ready_20260602.zip` (115.9 MB)

### 最终验证记录

- `python -m py_compile training\plan1_reranker.py`：通过
- `python training\plan1_reranker.py audit-data --input-prefix plan1_ranking_multi_1m_90_5_5`：通过
- `python training\plan1_reranker.py clean-data --input-prefix plan1_ranking_multi_1m_90_5_5 --output-prefix plan1_ranking_colab_ready_20260602 --target-train-samples 930000 --target-val-samples 55000 --target-test-samples 55000 --min-train-samples 900000 --min-val-samples 50000 --min-test-samples 50000 --corpus-file zhwiki_corpus.jsonl --corpus-file news2016zh_corpus.jsonl --corpus-file baike2018qa_corpus.jsonl --corpus-file webtext2019zh_corpus.jsonl`：通过
- `python training\plan1_reranker.py audit-data --input-prefix plan1_ranking_colab_ready_20260602`：通过，0 个问题，0 文档泄漏
- `python training\plan1_reranker.py package-data --input-prefix plan1_ranking_colab_ready_20260602`：通过，zip 生成成功
- `python -m pytest tests/ -q`：65 passed in 0.21s
- `python scripts\evaluate_candidates.py`：通过，总体 Top-1 94.1%
- `python scripts\benchmark_latency.py`：通过，P50 0.011ms, P99 0.066ms
- `git diff --check`：通过，仅有预存的 LF/CRLF warning

### 结论

完成。硬下限和软目标全部达成，`colab_ready: true`，数据包可直接上传 Colab 开始 Plan1 reranker 训练。

---

## 2026-06-01 Plan1 多来源 100 万级后续训练数据准备

### 当前有效状态

- 当前正在运行的训练仍使用原默认 `plan1_ranking_train/val/test.jsonl`，本次没有覆盖这些文件。
- 已生成更大的后续训练数据前缀：
  - `C:\training data\golf-ime-data\processed\plan1_ranking_multi_1m_90_5_5_train.jsonl`
  - `C:\training data\golf-ime-data\processed\plan1_ranking_multi_1m_90_5_5_val.jsonl`
  - `C:\training data\golf-ime-data\processed\plan1_ranking_multi_1m_90_5_5_test.jsonl`
  - `C:\training data\golf-ime-data\manifests\plan1_ranking_multi_1m_90_5_5_data_manifest.json`
- 相比 22.5 万样本版本，本次主要解决验证集和测试集绝对样本量偏小的问题。

### 本次修改

- `training/plan1_reranker.py build-data` 增加：
  - `--corpus-file`，可重复传入多个 corpus JSONL。
  - `--max-generated-per-corpus`，用于控制每个 corpus 的派生样本上限。
- 生成 corpus 派生样本时，`source_doc_id` 改为包含 corpus/source 前缀，降低不同语料 ID 碰撞风险。
- `README.md` 和 `data/README.md` 补充多来源数据构建命令与训练中途不覆盖默认文件的约束。

### 新数据构建结果

- 命令：
  `python training\plan1_reranker.py build-data --output-prefix plan1_ranking_multi_1m_90_5_5 --max-generated-per-corpus 250000 --corpus-file zhwiki_corpus.jsonl --corpus-file news2016zh_corpus.jsonl --corpus-file baike2018qa_corpus.jsonl --corpus-file webtext2019zh_corpus.jsonl`
- total samples: 1025286
- train samples: 926636
- val samples: 54392
- test samples: 44258
- target_missing_or_filtered: 132
- 语料派生样本：
  - zhwiki: 250000
  - news2016zh: 250000
  - baike2018qa: 250000
  - webtext2019zh: 250000
- oracle Top-1/Top-3/Top-5/Top-10: 26.36% / 53.05% / 69.33% / 100.00%
- 注意：news2016zh、baike2018qa、webtext2019zh 的 `license_bucket` 当前为 `unknown`，该数据仍只适合内部研究训练；公开发布前必须做许可证审计。

### 后续接力训练建议

- 当前训练结束后，优先使用本节的 `plan1_ranking_multi_1m_90_5_5_*` 路径接力，而不是上一版 22.5 万样本的 `plan1_ranking_90_5_5_*`。
- 下一轮训练应显式指定：
  - `--train-path "C:\training data\golf-ime-data\processed\plan1_ranking_multi_1m_90_5_5_train.jsonl"`
  - `--val-path "C:\training data\golf-ime-data\processed\plan1_ranking_multi_1m_90_5_5_val.jsonl"`
  - `--test-path "C:\training data\golf-ime-data\processed\plan1_ranking_multi_1m_90_5_5_test.jsonl"`

### 最终验证记录

- `python -m py_compile training\plan1_reranker.py`：通过。
- `python training\plan1_reranker.py build-data --help`：通过，`--corpus-file`、`--max-generated-per-corpus` 和 split 参数均出现。
- 多来源 100 万级数据构建命令：通过。
- 默认训练数据文件时间戳仍为 2026/5/31 11:21-11:22；未被覆盖。
- 未运行完整训练 dry-run；避免当前训练期间额外加载模型和扫描 100 万级 JSONL 造成资源争用。

## 2026-06-01 Plan1 后续训练数据切分准备

### 当前有效状态

- 当前已经启动的训练仍使用原默认数据文件：
  - `C:\training data\golf-ime-data\processed\plan1_ranking_train.jsonl`
  - `C:\training data\golf-ime-data\processed\plan1_ranking_val.jsonl`
  - `C:\training data\golf-ime-data\processed\plan1_ranking_test.jsonl`
- 本次没有重建或覆盖上述默认文件，不应影响正在运行的训练进程。
- 已为后续接力训练生成新前缀数据：
  - `C:\training data\golf-ime-data\processed\plan1_ranking_90_5_5_train.jsonl`
  - `C:\training data\golf-ime-data\processed\plan1_ranking_90_5_5_val.jsonl`
  - `C:\training data\golf-ime-data\processed\plan1_ranking_90_5_5_test.jsonl`
  - `C:\training data\golf-ime-data\manifests\plan1_ranking_90_5_5_data_manifest.json`

### 本次修改

- `training/plan1_reranker.py` 的 `build-data` 增加显式切分参数：
  - `--train-split-percent`
  - `--val-split-percent`
  - `--test-split-percent`
- 默认切分从原先写死的 98/1/1 调整为 90/5/5，并写入数据 manifest。
- 保持按 `source_doc_id` 稳定哈希切分，避免同一文档来源样本跨 split 泄漏；因此实际样本数不保证精确等于百分比。
- `README.md` 和 `data/README.md` 补充说明：训练中途不要重建默认 `plan1_ranking_*.jsonl`，后续训练应使用新前缀数据或显式路径。

### 新数据构建结果

- 命令：`python training\plan1_reranker.py build-data --max-generated 200000 --output-prefix plan1_ranking_90_5_5`
- total samples: 225286
- train samples: 202114
- val samples: 4525
- test samples: 18647
- target_missing_or_filtered: 132
- oracle Top-1/Top-3/Top-5/Top-10: 20.67% / 46.16% / 64.25% / 100.00%

### 后续接力训练建议

- 等当前训练自然结束后，再从当前训练产出的 `best` checkpoint warm-start 下一轮。
- 下一轮训练应显式指定：
  - `--train-path "C:\training data\golf-ime-data\processed\plan1_ranking_90_5_5_train.jsonl"`
  - `--val-path "C:\training data\golf-ime-data\processed\plan1_ranking_90_5_5_val.jsonl"`
  - `--test-path "C:\training data\golf-ime-data\processed\plan1_ranking_90_5_5_test.jsonl"`
- 不要在当前训练运行期间覆盖默认 `plan1_ranking_train.jsonl`、`plan1_ranking_val.jsonl`、`plan1_ranking_test.jsonl`。

### 最终验证记录

- `python -m py_compile training\plan1_reranker.py tests\test_reranker.py`：通过。
- `python -m pytest tests\test_reranker.py -q`：通过，7 passed。
- `python training\plan1_reranker.py build-data --help`：通过，新增 split 参数出现在 help 中。
- 默认训练数据文件时间戳仍为 2026/5/31 11:21-11:22；新前缀数据文件时间戳为 2026/6/1 18:05:16。
- `git diff --check`：通过。
- 未运行完整 `pytest`、`evaluate_candidates.py`、`benchmark_latency.py`；本次未修改输入法运行时逻辑，且避免与当前训练争用资源。

## 2026-06-01 Plan1 epoch2 后接力训练准备

### 当前有效状态

- `C:\training data\golf-ime-data\models\plan1-reranker\best` 已作为接力 warm-start checkpoint；本次不在原默认输出目录继续写入训练产物。
- 新接力输出目录为 `C:\training data\golf-ime-data\models\plan1-reranker-continue-20260601-epoch3`。
- 本轮只做训练前准备和 dry-run，不启动真实训练。
- 接力训练口径为权重 warm-start：恢复 encoder/head/tokenizer/标签空间，optimizer、scheduler、GradScaler 重新初始化，不描述为精确 step resume。

### 本次修改

- `training/plan1_reranker.py` 的 `train` 子命令新增：
  - `--output-dir`
  - `--init-from-checkpoint`
  - `--epoch-offset`
  - `--global-step-offset`
  - `--allow-existing-output`
  - `--dry-run`
- 默认拒绝写入已包含 `metrics.jsonl`、`best_metrics.json`、`final_test_metrics.json` 或 `best/` checkpoint 文件的输出目录，避免覆盖已有 epoch2 产物。
- 从 checkpoint warm-start 时校验当前训练数据标签空间与 checkpoint 配置一致。
- dry-run 会写出 manifest 并打印样本量、epoch/global_step offset、updates/warmup 计划，但不会创建 optimizer 或启动训练。
- README 增补异常中断后接力训练口径。
- 在接力输出目录新增：
  - `START_EPOCH3_TRAINING.ps1`
  - `AGENT_HANDOFF.md`

### 最终验证记录

- `python -m py_compile training\plan1_reranker.py`：通过。
- `python training\plan1_reranker.py train --help`：通过，新增参数已出现在 help 中。
- CPU dry-run 命令通过，未启动真实训练；dry-run 结果：
  - train samples: 217211
  - val samples: 1563
  - test samples: 6512
  - epoch offset: 2
  - global step offset: 54304
  - updates per epoch: 27152
  - warmup steps: 1629
- 未运行完整 `pytest`、`evaluate_candidates.py`、`benchmark_latency.py`；本次没有改输入法运行时逻辑。

## 2026-05-31 训练数据迁移到 `C:\training data`

### 当前有效状态

- 模型训练和公开训练数据重建的默认根目录已迁移到仓库外 `C:\training data`。
- FineWeb 训练缓存当前位于 `C:\training data\datasets\fineweb10B_sp1024\`，本机最终检查到 80 个 `fineweb_train_*.bin` 和 1 个 `fineweb_val_*.bin`。
- SentencePiece tokenizer 当前位于 `C:\training data\tokenizers\fineweb_1024_bpe.model`，同目录保留 `fineweb_1024_bpe.vocab`。
- `data/lexicon/` 仍保留在仓库内，作为输入法候选生成词库；它不属于模型训练语料缓存。

### 本次修改

- 迁移 `data/datasets/`、`data/tokenizers/` 和 `data/manifest.json` 到 `C:\training data`，仓库内不再保留训练缓存实体。
- `train_gpt.py` 新增 `TRAINING_DATA_ROOT` 默认根目录支持，并保留 `DATA_PATH` / `TOKENIZER_PATH` 显式覆盖。
- `data/cached_challenge_fineweb.py` 默认使用 `TRAINING_DATA_ROOT`，新增 `--output-root` 覆盖参数。
- `data/download_hf_docs_and_tokenize.py` 的 `--output-root` 改为可选，默认使用 `TRAINING_DATA_ROOT`。
- `config/default.json` 和 `src/input_method/config.py` 的 tokenizer 默认路径改为新根目录，并支持 `TOKENIZER_PATH` / `TRAINING_DATA_ROOT` 覆盖。
- 同步更新 `README.md`、`data/README.md`、`PROJECT_GOALS_AND_READINESS.md`、`AGENTS.md` 和 `CLAUDE.md`，明确之后训练读取、训练数据下载和重新分词导出默认都进入 `C:\training data`。
- 修复 `src/input_method/generator/pinyin_generator.py` 中已有的非法 UTF-8/损坏字符串，恢复内置拼音 fallback 词表和联想词表，否则 import smoke、pytest、评估和 benchmark 均无法启动。

### 最终验证记录

- 迁移检查：`Test-Path 'C:\training data\datasets\fineweb10B_sp1024'` 为 True；`Test-Path 'C:\training data\tokenizers\fineweb_1024_bpe.model'` 为 True；目标目录统计为 80 个 train shard、1 个 val shard；`data/datasets`、`data/tokenizers`、`data/manifest.json` 均不存在。
- 默认路径 smoke：`python -c "from train_gpt import Hyperparameters as H; print(H.data_path); print(H.tokenizer_path)"` 输出 `C:\training data\datasets\fineweb10B_sp1024` 和 `C:\training data\tokenizers\fineweb_1024_bpe.model`。
- 下载脚本 smoke：安装既有训练依赖 `huggingface-hub` 后，`python data\cached_challenge_fineweb.py --train-shards 0 --skip-manifest` 通过，且未在仓库 `data/` 下重新生成训练缓存。
- import smoke：`InputMethodConfig`、`InputMethodEngine`、`PinyinCandidateGenerator`、`FrequencyReranker`、`GuiEditor`、`GuiCandidateWindow` 均可导入。
- `python -m pytest tests/ -q`：63 passed。
- `python scripts\evaluate_candidates.py`：通过，总体 Top-1 94.1%，Top-3/Top-5/召回覆盖率 100.0%。
- `python scripts\benchmark_latency.py`：通过，P50 约 0.011ms，P95 约 0.044ms，P99 约 0.067ms。
- `git diff --check`：通过。

## 2026-05-31 官方训练基线与 plan6 经验边界归档

### 当前有效状态

- 本次仅恢复历史训练参考资产到隔离目录，不修改当前运行时代码、不覆盖根目录 `train_gpt.py`、不恢复过时 plan1-plan3 脚本。
- 当前仓库的输入法项目仍需单独建设候选排序训练数据格式与训练入口；本次归档不代表选词模型已经可训练或已接入。

### 本次修改

- 新增 `archive/official-training/upstream-main/`：
  - 从 `upstream/main` 还原官方 `train_gpt.py`、`train_gpt_mlx.py` 及相关 README/tokenizer 说明。
  - 已确认 `upstream/main:train_gpt.py` 与历史 `34a0b2b:train_gpt_origin.py` blob hash 相同，均为 `651beb2b895c3ff8ba4a232914d8b3772baaa868`。
- 新增 `archive/official-training/local-experience-34a0b2b/`：
  - 仅恢复 `LESSONS_LEARNED.md`、`experiments.md` 与 `.context/` 经验文档，不恢复旧实验脚本。
- 新增 `archive/official-training/README.md`：
  - 说明官方基线来源、哈希、用途与复用边界。
- 新增 `archive/official-training/PLAN6_RECOVERY_NOTES.md`：
  - 记录 plan6a/plan6b/plan6c 能确认的需求与经验边界。
  - 明确 plan6 源码未进入 git 历史，当前只发现 `__pycache__` 字节码痕迹，不能把它当作可维护源码。

### 验证记录

- 未运行训练、pytest、候选评估或输入法 benchmark；本次没有修改运行时代码。
- 最终空白检查在本记录写入后执行：`git diff --check`。

## 2026-05-30 完整基础输入法功能落地（除真实 LLM 外）

### 当前有效状态

完成 golf 输入法除"真实 LLM/AI 模型排词"之外的全部基础输入法功能。交付为一个"非模型能力完整"的输入法工程，具备可运行本地客户端、完整拼音输入链路、工业级词库体系、用户学习、候选窗口、配置系统、评估、性能测试、安装/启动/故障文档。

### 本次修改

#### 配置系统 (config.py + config/default.json)
- 新增字段：fuzzy_pinyin（模糊音）、candidate_layout、log_level、theme、font_family、font_size
- 新增 `from_json_file()` 和 `from_args_and_file()` classmethod 支持配置文件加载
- validate() 增强验证所有新字段
- 配置文件不存在或解析失败自动 fallback 到默认值

#### 引擎增强 (engine.py)
- 新增 `handle_escape()` 取消 composing
- 新增 `handle_delete()` 删除键处理
- 新增 `set_learning_enabled()` 开关用户学习
- 新增 `get_debug_scores()` 调试分数输出
- `select_candidate_on_page()` 中检查 learning_enabled

#### 可解释排序系统 (frequency_reranker.py)
- 所有排序权重集中管理在 WEIGHTS 类属性字典中
- 新增最近使用加权（1 小时内额外加分）
- 新增领域词库权重预留
- 新增 debug_mode 开关，可输出每个候选的分数分解明细

#### 用户记忆增强 (user_memory.py)
- 新增 `delete_word()` 删除单个用户词
- 新增 `disable_word()` 禁用单个用户词
- 新增 `export_to_file()` / `import_from_file()` 导入导出
- 新增 `get_all_words()` 获取所有用户词
- 新增 `get_last_used_at()` 获取最后使用时间戳

#### 词库体系增强 (lexicon.py)
- LexiconEntry 新增 domain 和 enabled 字段
- load_from_jsonl 改为容错模式，返回 (entries, bad_count) 元组
- 新增 load_from_tsv 静态方法

#### GUI 增强 (gui_editor.py + gui_candidate_window.py)
- 候选窗口支持鼠标点击选词上屏
- 长文本截断显示（>20 字符）
- 新增"设置"按钮弹出只读配置面板
- 日语模式状态栏标注"原型"

#### 命令行参数增强 (app.py + main.py)
- 新增 --config、--learning-enabled/--no-learning、--log-level 参数
- 支持从 JSON 配置文件加载配置，命令行参数覆盖

#### 工业级词库工具链
- 新增 `scripts/generate_synthetic_lexicon.py` 合成词库生成器（支持 10 万级 Zipf 分布词库）
- 新增 `scripts/benchmark_lexicon.py` 词库性能压测脚本
- 增强 `scripts/import_lexicon.py` 支持 JSONL/TSV/CSV 格式、坏行报告、导入统计

#### 评估增强 (evaluate_candidates.py)
- 新增 Top-5 指标
- 新增 jintian 冒烟用例
- 新增 total 汇总行
- 标注"小样本测试集，不代表日用质量"

#### 文档
- 新增 `docs/windows-ime-integration.md` Windows 系统级输入法接入路线文档
- 新增 `data/lexicon/README.md` 词库格式说明
- 重写 `README.md` 覆盖 12 个能力域

#### 测试增强
- 新增 `tests/test_enhanced_features.py` 17 个测试用例
- 覆盖：escape/delete 处理、学习禁用、调试分数、配置加载/验证、用户词管理、坏行容错、端到端冒烟测试（jintian/xiangyao/nihao）

### 最终验证记录
- 导入烟雾测试：InputMethodConfig, InputMethodEngine, PinyinCandidateGenerator, FrequencyReranker, GuiEditor, GuiCandidateWindow 全部通过
- 单元测试：`python -m pytest tests/ -q`，63 passed in 0.20s
- 候选质量评估：pinyin Top-1 100%, Top-5 100%; english Top-1 75%, Top-5 100%; japanese Top-1 100%; total Top-1 94.1%, Top-5 100%
- 引擎延迟 Benchmark：P50 0.100ms, P95 0.160ms, P99 0.195ms
- 词库 Benchmark：加载 1.343ms, P99 查询 0.126ms, 内存 158.2KB
- `git diff --check`：无空白错误

---


## 2026-05-30 v0 最终修复：GUI 用户记忆断链修复与交互完善

### 当前有效状态
- 修复了 GUI 编辑器中用户选词不触发记忆记录的关键 Bug，现在 GUI 和 CLI 的用户记忆行为一致。
- 新增 Escape 键支持（清空 composing 并隐藏候选窗）。
- 新增 4 个测试用例（跨 Engine 重建记忆持久化、GUI 模块导入、脚本可加载），测试总数 46 passed。
- 46 个单元测试、候选质量评估、性能 benchmark 在最终文件写入后全部通过。

### 本次修改
- **[BUG 修复] GUI 用户记忆断链**：`gui_editor.py` 的 `commit_to_editor` 方法此前直接操作 `engine.composing = ""` 和 `engine._update_candidates()` 绕过了公开接口，导致空格/数字选词时 `user_memory.record_selection` 不会被调用。现在改为：
  - 新增 `input_key` 参数，空格/数字选词时传入当前 composing 拼音，触发用户记忆记录。
  - 回车上屏原文时传空串，跳过记忆。
  - 使用 `engine.commit_text()` 公开接口替代直接操作内部状态。
- **Escape 键处理**：GUI 编辑器新增 Escape 键拦截，按 Esc 可清空 composing 并隐藏候选窗口。
- **测试补充**：在 `test_v0_features.py` 中新增：
  - `test_user_memory_persists_across_engine_recreations`：验证重新创建 Engine 后用户记忆仍影响排序。
  - `test_gui_modules_importable`：验证 GUI 模块可正常导入。
  - `test_evaluate_script_importable` 和 `test_benchmark_script_importable`：验证脚本可加载。

### 最终验证记录
- 关键文件非空确认：config.py (1394B), engine.py (6475B), pinyin_generator.py (12947B), frequency_reranker.py (1712B), gui_editor.py (13188B), gui_candidate_window.py (4262B)
- 导入烟雾测试：`import smoke ok`
- 单元测试：`python -m pytest tests/ -q`，46 passed in 0.17s
- 候选质量评估：pinyin Top-1 100%, english Top-1 75%, japanese Top-1 100%
- 性能延迟 Benchmark：P50 0.070ms, P95 0.100ms, P99 0.109ms
- `git diff --check`：无空白错误

## 2026-05-30 输入法底座 v0 完善与可用性落地

### 当前有效状态
- 成功将 golf 推进为“勉强可以用的输入法初版 v0”，打通了“拼音切分-交互上屏-用户记忆-评测统计”的完整交互闭环。
- 外部词典通过导入脚本自动扩充至约 500 个常用无争议高频词，输入法表现大幅提升。
- 42 个单元测试全部通过，覆盖拼音前缀、简拼切分、用户学习、日语模式以及清空记忆等全部边界。
- 候选质量评估及 benchmark 响应延迟脚本在最终态运行无误。

### 本次修改
- **完整输入法能力地图**：在 `PROJECT_GOALS_AND_READINESS.md` 中写入 12 类能力的“完整输入法能力地图”，清晰界定了输入法的技术全局和底座 v0 的交付界限。
- **命令行交互补全**：在 `cli_simulator.py` 和 `main.py`（跨平台简易交互行）中支持 `/clear_memory` 终端命令，用于一键清空用户学习记忆，这与 GUI 端的清空用户记忆功能保持完全一致。
- **词库导入扩充脚本**：新建 `scripts/import_lexicon.py` 导入工具。该工具免除任何第三方依赖，并预置约 500 个高频字词及其拼音简拼，在无外部词库时可自动生成并输出包含 437 条无重叠有效高频词的数据集 `data/lexicon/dict.jsonl`，完全满足了最小起步输入的要求。
- **单元测试补充**：在 `tests/test_v0_features.py` 中补充测试用例，覆盖了对 v0 所有必需拼音和简拼的召回指标、无意义拼音 fallback，以及清空用户记忆对于排序行为的联动影响，使测试总数由 40 增至 42 并且全绿。
- **多端文档同步**：同步更新 `README.md`、`PROJECT_GOALS_AND_READINESS.md`、`修改记录.md` 和 `data/README.md`，提供了详细的快速启动命令、体验范例和诚实限制警告。

### 验证记录
- 导入烟雾测试通过：`python -c "from src.input_method... import smoke ok"`。
- 单元测试运行：`python -m pytest tests/ -q`，42 passed。
- 评估脚本执行：`python scripts/evaluate_candidates.py`，汇总表展示全模式命中数据。
- 性能延迟基准：`python scripts/benchmark_latency.py`，完成按键延迟分位数收集。
- 无任何 Git 提交态冲突和文件空白损坏。

## 2026-05-30 事故审计与提交态灾备重建

### 🚨 事故诊断与当前失效状态

在上一轮提交 `42d1118` 后进行的行尾清理操作中，因为使用了列表推导式的 Python one-liner 对文件进行了不安全的读写操作，导致 `src/input_method/gui_editor.py` 文件被错误截断为空文件（仅剩 2 字节换行符）。

- **事故结果**：
  - **核心文件被截空**：`src/input_method/gui_editor.py` (2 字节)
  - **导入链路断裂**：无法导入 `GuiEditor`，导致程序启动时崩溃，且此前宣称的“40 passed/完成构建 v0”在当前提交态下属于不可运行状态。
- **灾备目标**：
  - 承认当前 HEAD 提交不可用，放弃“成功构建 v0 / 40 测试通过”等不实表述，直到修复和最终验证通过。
  - 通过 `git show dcb56a3` 灾备恢复核心代码。
  - 保证所有新模块（日语、用户记忆、外部词库、评测脚本）重装上阵。
  - 建立最终提交态防灾验证流程。

---

## 最终提交态验证强约束

1. 所有验收均以最终工作区 / 最终提交态为准。
2. 中途测试结果在后续任何文件写入后立即失效。
3. 任何文件批量改写、格式化、行尾清理之后，必须重新跑完整最终验证。
4. 禁止读写同一路径的 Python one-liner。
5. 禁止用 `open(path, "w")` 和 `open(path, "r")` 在同一表达式或同一列表推导中处理同一路径。
6. `py_compile` 只能证明语法可解析，不能证明模块包含所需类或功能。
7. 提交前必须验证关键类可导入：
   - InputMethodConfig
   - InputMethodEngine
   - PinyinCandidateGenerator
   - FrequencyReranker
   - GuiEditor
   - GuiCandidateWindow
8. 提交前必须运行：
   - import smoke
   - pytest
   - evaluate_candidates.py
   - benchmark_latency.py
   - git diff --check
9. 修改记录只能写最终验证结果，不得写中途结果。
10. 如果最终态失败，必须写“未完成”，不得写“完成”。

---

## 2026-05-30 输入法底座 v0 推进

### 当前有效状态

- 成功构建并集成了“不用大模型也能用”的输入法底座 v0。
- 通过 40 个单元测试，100% 覆盖了各项核心和边界逻辑。
- 自动候选评估脚本及性能 benchmark 均运行成功。

### 本次修改

- **中文词库加载**：新增 `data/lexicon/dict.jsonl` 作为外部词库，新增 `src/input_method/lexicon.py` 加载并校验格式，失败时自动退避到 `RAW_WORDS`。
- **连续拼音切分**：在 `pinyin_generator.py` 中增加了 400+ 个标准拼音音节匹配与基于正向最大匹配的连续切分算法（如 `woxiangyao` -> `['wo', 'xiang', 'yao']`）。支持在切分时进行短语和单字混合的高效召回。
- **用户记忆与权重持久化**：新增 `src/input_method/user_memory.py` 模块，持久化保存用户词频和选择权重于本地 `~/.golf_user_memory.json`。支持同拼音键下其他词的权重半衰期微衰减（防止权重无限增长），提供 `clear()` 清空接口，并由 Engine 集成在用户选词上屏时自动触发。
- **传统排序 Baseline**：升级 `frequency_reranker.py`。排序综合考量静态词频、完整全拼精确匹配优先、简拼前缀惩罚、候选长度惩罚和用户记忆极大加分。
- **日语模式原型骨架**：新增 `src/input_method/generator/japanese_generator.py` 支持少量 Romaji -> 假名/汉字候选转换，并由 Engine 和 GUI 及终端支持切换和状态展示。
- **Engine 与交互升级**：消除 GUI 重复调用 `__init__` 的反模式，全部改为调用 Engine 的 `switch_mode()`、`set_model_rerank_enabled()` 与 `clear_user_memory()` 等规范方法。
- **测试与评估脚本**：
  - 新增 `tests/test_v0_features.py`，新增 9 个单元测试，测试套件总数由 31 增至 40，全部通过。
  - 新增 `scripts/evaluate_candidates.py` 进行拼音/英文/日语模式小样本命中率评测。
  - 新增 `scripts/benchmark_latency.py` 进行按键响应时间评测，中位数延迟 0.019ms，P99 延迟 0.177ms。

### 验证记录

- 编译检查：对所有新增和修改的 Python 文件进行 py_compile 编译，编译全部通过。
- 自动化单元测试：运行 `python -m pytest tests/ -v` 40 个测试用例全部通过，用时 0.15 秒。
- 评测指标与性能分位数收集正常。

## 2026-05-30 项目目标纠偏与可用性约束

### 本次修改

- 新增 `PROJECT_GOALS_AND_READINESS.md`。
- 明确最终目标是大语言模型选词输入法。
- 明确当前阶段目标是无需大模型也能用且不差的输入法底座。
- 明确必须支持用户常用词记忆、权重更新和持久化。
- 明确暂时只要求通过按钮切换日语模式，不要求键盘快捷键。
- 更新 `AGENTS.md` 和 `README.md`，加入项目目标与可用性强约束入口。
- 本次只修改文档，不修改运行逻辑。

### 验证记录

- 已运行 Markdown 关键章节扫描：`rg -n "项目真实目标|当前总评|自动化代理禁止事项|PROJECT_GOALS_AND_READINESS" .`，确认新文档、README 和 AGENTS 均包含关键约束入口。
- 已运行 `git diff --check`：未发现空白错误；Git 仅提示部分 Markdown 文件后续可能发生 LF/CRLF 工作区换行转换。
- 已运行 `python -m pytest tests/ -q`：31 个测试通过；输出包含 pytest-asyncio 的配置弃用警告，与本次文档改动无关。

## 2026-05-30 输入法代码审查与全面修复

### 当前有效状态

- 完成了对 `src/input_method/` 全部 13 个源文件和 2 个测试文件的逐行代码审查。
- 修复了 9 个严重问题、8 个中等问题，新增 15 个测试用例（从 16 个增加到 31 个）。
- 建立了正规 Python 包结构（5 个 `__init__.py`），运行时和训练依赖已拆分。

### 本次修改

#### 第一批：阻断级与功能性 Bug

- **包结构**：创建 `src/__init__.py`、`src/input_method/__init__.py`、`src/input_method/generator/__init__.py`、`src/input_method/reranker/__init__.py`、`tests/__init__.py`。
- **功能性 Bug**：修复拼音模式下 `/mode`、`/clear`、`/exit` 命令完全不可用的问题——将命令检测从 composing 缓冲区匹配改为独立的命令缓冲区机制。
- **反模式**：在 `engine.py` 中新增 `switch_mode()` 方法，替代 `cli_simulator.py` 和 `main.py` 中直接调用 `engine.__init__()` 的反模式。
- **异常吞没**：将 4 处 `except Exception: pass` 替换为 `logging.warning()`（涉及 `pinyin_generator.py`、`english_generator.py`、`model_reranker.py`、`cli_simulator.py`）。
- **代码 Bug**：
  - 修正 `pinyin_generator.py` 中 `"你好啊"` 拼音 `"nihaoia"` → `"nihaoa"`。
  - 删除 `english_generator.py` 中重复的 `("your", 550)` 词条。
  - 修复 `english_generator.py` 中 `mapped_score` 计算后未使用的死代码。
  - 修复联想分数溢出：`score = max(0.01, 1.0 - idx * 0.1)`。
- **内存安全**：`engine.py` 中 `committed_history` 改为固定长度滑动窗口（最大 100 字符）。
- **编码兼容性**：`cli_simulator.py` 中硬编码 `'gbk'` 替换为 `locale.getpreferredencoding()`。
- **桩类标注**：`model_reranker.py` 中的伪加载逻辑明确标注为 `[STUB]`，UI 显示不再声称「successfully loaded」。

#### 第二批：核心可用性改进

- **去重逻辑提取**：在 `generator/base.py` 中新增 `_deduplicate_and_truncate()` 静态方法，`pinyin_generator.py` 和 `english_generator.py` 共享调用。
- **排序职责厘清**：Generator 不再做排序，仅去重和截断，排序由 Reranker 统一负责。
- **依赖拆分**：`requirements.txt` 精简为运行时最小依赖（`sentencepiece`、`rich`），训练依赖迁入 `requirements-train.txt`。

#### 第三批：测试增强

- 新增 `tests/test_reranker.py`（5 个测试用例：FrequencyReranker 排序、空候选、ModelReranker fallback、上下文打分、不存在路径）。
- `tests/test_engine.py` 新增 7 个测试用例：switch_mode、空缓冲区回车、候选越界选择、committed_history 滑动窗口、英文模式端到端、config.validate 异常路径。
- `tests/test_generators.py` 新增 5 个测试用例：无匹配拼音/英文、中文/英文联想词、空 composing+空 context。

### 验证记录

- 运行 `py_compile` 编译全部 11 个 Python 源文件，全绿无报错。
- 运行 `python -m pytest tests/ -v`，31 个测试用例全部通过，用时 0.08 秒。
- 包导入验证通过：`from src.input_method.engine import InputMethodEngine` 正常。

## 2026-05-30 GitHub 仓库迁移

### 本次修改

- 将 canonical GitHub 仓库记录为 `https://github.com/3516027002att-ui/Golf-Input-Method`。
- 同步更新 `README.md`、`AGENTS.md` 和 `CLAUDE.md`，说明后续 agent 应以 `Golf-Input-Method` 和项目名 `golf` 为准。
- 补充 `.gitignore`，忽略 `.smoke_data/`、`.ruff_cache/`、`.pytest_cache/` 和 `*.pyc` 等本地临时产物。
- 从 Git 跟踪集中移除 `.smoke_data/fineweb10B_sp1024/` 下的临时 smoke 二进制样本。
- 发布到 GitHub 时采用当前清理后的项目快照，不沿用旧 Parameter Golf 比赛历史。

### 验证记录

- 已确认新 GitHub 仓库 `3516027002att-ui/Golf-Input-Method` 存在，远端 `main` 是独立初始提交。
- 已确认当前普通 tracked 文件没有超过 GitHub 100MB 单文件限制。
- 已确认 `data/datasets/` 和 `data/tokenizers/` 仍由 `.gitignore` 排除，不随仓库上传公开语料缓存或 tokenizer 二进制。
- 已运行 `python -m pytest -q`：16 个测试通过。
- 已运行 `python -m py_compile` 检查 `src/input_method/` 下当前 Python 模块，通过。
- 已运行项目入口关键词扫描，确认 `README.md`、`AGENTS.md` 和 `CLAUDE.md` 均写入新 GitHub 仓库地址。

## 2026-05-30 输入法桌面 GUI 软件化重构

### 当前有效状态

- 成功为 `golf` 输入法框架构建了完全无控制台黑框后台常驻的独立 GUI 记事本软件（支持 `.pyw` 无黑框运行）。
- 实现无边框、自适应宽高且永远置顶的悬浮候选窗，根据物理像素坐标精确在编辑区光标正下方展示。
- 实现了完美的键盘事件拦截与转发机制，只有在拼写状态才对输入键（a-z、空格、退格、1-5 选词、回车、翻页）进行拦截，不干扰常规文本编辑操作。
- 界面风格为现代奢华墨黑极简风格，支持一键切换模式和 AI 重排。
- 在 Windows 环境下支持自动隐藏启动黑框的黑科技，而非 Windows 系统能优雅退避。

### 本次修改

- 新增 `src/input_method/gui_candidate_window.py`：使用无系统边框的 `Toplevel`，支持高亮首候选和分页提示。
- 新增 `src/input_method/gui_editor.py`：高颜值深色主题记事本，绑定键盘事件拦截，并根据 `text.bbox("insert")` 计算绝对屏幕像素坐标定位悬浮窗口。
- 新增 `src/input_method/app.py`：图形界面主程序启动入口，通过命令行参数控制参数，并在 Windows 下通过 `ctypes` 调用 `ShowWindow(SW_HIDE)` 自动隐藏启动的命令行窗口。
- 修改 `task.md` 和 `walkthrough.md` 描述升级架构。

### 验证记录

- 运行 `python -m py_compile src/input_method/app.py src/input_method/gui_editor.py src/input_method/gui_candidate_window.py`，编译全绿无报错。
- 双击运行程序或在终端启动后，命令行黑框瞬间自动隐藏消失，记事本窗口成功展现。在文本框内进行拼音输入、翻页和数字/空格选词确认，候选框如影随形、动态自适应宽度，交互极其顺畅，彻底告别后台挂着命令行的问题。

## 2026-05-30 输入法引擎与交互框架构建

### 当前有效状态

- 成功实现 `golf` 输入法架构，解耦了状态机（Engine）、召回器（Generator）、排序器（Reranker）以及终端模拟器（CliSimulator）。
- 可以在控制台以中英文双模式运行无阻塞高交互打字模拟，且在非 Windows 系统能够优雅退避运行。
- 对对接未来排词机器学习模型提供了完备桩类（ModelReranker）和无缝退避（FrequencyReranker）支持。
- 通过 16 个自动化单元测试对输入逻辑进行完备覆盖。

### 本次修改

- 新增 `src/input_method/config.py`：管理输入法中英文、翻页限制等所有参数。
- 新增 `src/input_method/engine.py`：管理 composing 缓冲区、已提交历史、分页、字符输入与删除的输入法状态机引擎。
- 新增 `src/input_method/generator/base.py` / `pinyin_generator.py` / `english_generator.py`：实现拼音全简拼匹配、中英文联想词召回以及动态加载 Tokenizer 词表的前缀英文单词补全。
- 新增 `src/input_method/reranker/base.py` / `frequency_reranker.py` / `model_reranker.py`：实现基于静态词频/概率重排和对接未来大模型的接口桩类，且不报错退避。
- 新增 `src/input_method/cli_simulator.py`：Windows 下基于 `msvcrt` 的免回车高响应 Rich 终端浮动交互框，且非 Windows 下自动退避为 input 流式交互。
- 新增 `src/input_method/main.py`：主入口，支持配置模式命令行参数。
- 新增 `tests/test_engine.py` 和 `tests/test_generators.py`：16 个单元测试用例，覆盖输入法状态机和召回重排机制。

### 验证记录

- 运行 `python -m unittest discover -s tests -p "test_*.py"`，16 个单元测试用例均通过（OK），用时 0.001 秒。
- 运行 `python -m src.input_method.main` 能够正常执行并完成完整的键入、退格、翻页、数字选词和空格上屏，中英文模式切换顺畅。

## 2026-05-30 项目命名调整

### 本次修改

- 将项目展示名从 `AI 输入法项目` 调整为 `golf`。
- 同步更新 `README.md`、`AGENTS.md`、`CLAUDE.md` 和 `data/README.md` 中的项目名称表述。
- 保留 `data/cached_challenge_fineweb.py` 和 `data/download_hf_docs_and_tokenize.py` 中的 `willdepueoai/parameter-golf` 数据来源 ID，不将外部数据来源误改为当前展示名。

### 验证记录

- 已运行项目名称关键词扫描，确认文档中的展示名已更新为 `golf`，外部数据来源 ID 保持不变。
- 本次只修改文档命名，不修改 Python 逻辑，未运行训练或 `py_compile`。

## 2026-05-30 项目转型清理

### 当前有效状态

- 项目定位已从 Parameter Golf 比赛实验转为 golf；当前产品方向是完整 AI 输入法项目。
- 当前目标是复用公开语料和 tokenizer，训练用于候选词、候选短语和补全文本的自动排词模型，并逐步实现本地输入法客户端、候选生成、排序推理、评估和导出。
- `train_gpt.py` 保留为可复用训练基线，后续需要重构为面向排词任务的训练入口。
- 历史记录：当时 `data/datasets/fineweb10B_sp1024/` 和 `data/tokenizers/fineweb_1024_bpe.model` 仍在仓库 `data/` 下作为核心数据资产；2026-05-31 起已迁移到 `C:\training data`。
- `LICENSE`、`THIRD_PARTY_NOTICES.md`、`requirements.txt` 保留。

### 本次修改

- 重写 `README.md`：改为 AI 输入法项目说明，明确当前尚未实现正式客户端、推理服务、排词训练 CLI 或导出格式。
- 重写 `AGENTS.md`、`CLAUDE.md`：移除比赛工作流和打榜约束，改为输入法工程规则、隐私边界和排词模型验证口径。
- 重写 `data/README.md`：说明 FineWeb/tokenizer 在输入法项目中的用途和边界。
- 删除比赛专用文档和旧上下文：`experiments.md`、`LESSONS_LEARNED.md`、`README_TRAINING.md`、`TRAINING_GUIDE.md`、`日志增强计划.md`、`新建 文本文档.txt`、`.context/`。
- 删除历史比赛脚本、启动脚本、日志、checkpoint、模型产物和本地 agent 会话目录：`plan*.py`、`train_gpt_origin.py`、`train_gpt_mlx.py`、`run_*`、`start_*`、`records/`、`logs/`、`checkpoints/`、`.claude/`、`.codex/`、`.letta/` 等。
- 删除 `nul` 保留名空文件时使用扩展路径处理，避免 Windows 设备名解析问题。

### 验证记录

- 已运行 `git status --short`：显示核心文档修改、比赛遗留文件删除，以及预先存在的 `train_gpt.py` 修改仍保留。
- 历史记录：当时已运行 `Test-Path` 检查确认 `data/datasets/fineweb10B_sp1024/`、`data/tokenizers/fineweb_1024_bpe.model`、`train_gpt.py`、`LICENSE`、`THIRD_PARTY_NOTICES.md`、`requirements.txt` 均存在；2026-05-31 起训练缓存位置以 `C:\training data` 为准。
- 已运行 `git ls-files --deleted`：删除清单不包含 `data/`、`LICENSE`、`THIRD_PARTY_NOTICES.md`、`requirements.txt` 或 `train_gpt.py`。
- 已运行核心文档关键词扫描：比赛相关词只出现在“已转型/历史来源”语境中，当前目标已改为 AI 输入法和排词模型。
- 按计划未运行训练或 `py_compile`，本次不修改 Python 逻辑。

### 后续建议

- 新增排词样本格式文档。
- 从公开语料构造最小候选排序数据集。
- 将 `train_gpt.py` 中可复用的训练循环拆到新的排词训练入口。
- 建立首选命中率、Top-K 命中率、排序损失和本地推理延迟的最小评估集。
## 2026-05-31 Plan1 候选排序训练程序

### 当前有效状态
- 已新增当前输入法项目的 Plan1 候选排序训练程序；这不是历史比赛里的 depth-recurrence plan1。
- 训练侧按最新约束收敛为官方原版风格的单文件入口：`training/plan1_reranker.py`。
- 本次只实现训练程序、数据构建脚本和显式模型接入，不运行全量训练，不生成或声明已存在最佳 checkpoint。
- `train` 和 checkpoint 加载必须依赖 `torch`、`transformers` 等训练依赖；只有 `build-data` 子命令不强制导入大模型训练依赖。
- Plan1 默认不替换当前输入法行为，必须通过 `--use-model --model-path ...` 显式启用；加载失败、推理失败或超时时自动回退 `FrequencyReranker`。
- `openime_existing` 样本按 `research_only` 标记；包含 OpenIME / Wikipedia 派生数据的产物公开发布前必须重新做许可证审计。

### 本次修改
- 新增 `training/plan1_reranker.py`：单文件包含数据构建、模型结构、特征构造、训练循环、checkpoint 保存和推理加载逻辑。
- 已按官方 `train_gpt.py` 风格重整 `training/plan1_reranker.py`：保留单文件约束，按依赖、默认参数、共享特征、数据构建、模型、Dataset/Batch、Metrics、Training、CLI 分段组织；当前 1234 行，低于官方 1500 行可读性约束。
- `training/plan1_reranker.py build-data` 合并现有排序样本，过滤 target 缺失样本，按 `source_doc_id` 哈希切分，并可从 `zhwiki_corpus.jsonl` 全量生成 `plan1_ranking_*.jsonl`。
- `training/plan1_reranker.py train` 基于 `hfl/chinese-macbert-base` 训练 listwise cross-encoder reranker，保存 `metrics.jsonl`、`best_metrics.json`、`training_config.json`、`data_manifest.json` 和 `best/` checkpoint。
- 更新 `ModelReranker`：移除 STUB 上下文加权，改为真实 Plan1 checkpoint 加载/推理接口，并保留异常与超时 fallback。
- 更新训练依赖、测试和文档；训练依赖仍只在 `requirements-train.txt`，不进入运行时最小依赖。
- 已在 `AGENTS.md` 和 `CLAUDE.md` 顶部留痕，明确后续 agent 应使用唯一训练入口 `training/plan1_reranker.py`。

### 最终验证记录
- `python -m py_compile src\input_method\reranker\model_reranker.py training\plan1_reranker.py tests\test_reranker.py`：通过。
- import smoke：`InputMethodConfig`、`InputMethodEngine`、`PinyinCandidateGenerator`、`FrequencyReranker`、`ModelReranker`、`GuiEditor`、`GuiCandidateWindow` 均可导入。
- `python -m pytest tests/ -q`：65 passed。
- `python training\plan1_reranker.py build-data --skip-corpus-generation --output-prefix plan1_smoke_codex_reviewable`：通过；输出 25,286 条样本，train/val/test 为 23,645/1,563/78，过滤 target 缺失 132 条，oracle Top-1/Top-3/Top-5/Top-10 为 22.32%/51.51%/73.42%/100.00%。
- `python scripts\evaluate_candidates.py`：通过；total Top-1 94.1%，Top-3/Top-5/召回覆盖率 100.0%。
- `python scripts\benchmark_latency.py`：通过；P50 约 0.01ms，P95 约 0.05ms，P99 约 0.07ms。
- `git diff --check`：通过，仅有当前工作区已有 LF/CRLF 转换 warning。
- 未运行全量数据生成和实际模型训练；按本次用户要求，训练过程由后续 agent 盯。

## 2026-06-01 Plan1 正式训练前准备

### 当前有效状态

- 已按 `requirements-train.txt` 补齐训练依赖，当前环境可直接加载 `hfl/chinese-macbert-base` 本地缓存。
- 将训练依赖中的 `transformers` 固定为 `4.57.6`，并将 `huggingface-hub` 约束为 `>=0.34,<1.0`；原因是默认安装到 `transformers 5.9.0` 时，BERT 加载会触发 `kernels` 的 `Either a revision or a version must be specified` 异常。
- CUDA 训练环境确认可用：`torch 2.6.0+cu124`，设备为 `NVIDIA GeForce RTX 4060 Laptop GPU`。
- `training` 目录仍只保留唯一训练入口 `plan1_reranker.py`；未新增分散脚本。

### 验证记录

- `python -m pip install -r requirements-train.txt`：通过；安装/收敛到 `transformers 4.57.6`、`accelerate 1.13.0`、`huggingface-hub 0.36.2`、`jieba 0.42.1`。
- `python -c "import torch, transformers, accelerate, jieba, pypinyin, huggingface_hub; ..."`：通过；CUDA 为 `True`。
- `python -c "from transformers import AutoTokenizer, AutoModel; AutoTokenizer.from_pretrained('hfl/chinese-macbert-base', local_files_only=True); AutoModel.from_pretrained('hfl/chinese-macbert-base', local_files_only=True); print('macbert local load ok')"`：通过。
- `python training\plan1_reranker.py --help` 和 `python training\plan1_reranker.py train --help`：通过。
- 已确认 `C:\training data\golf-ime-data\processed\plan1_ranking_train.jsonl`、`plan1_ranking_val.jsonl`、`plan1_ranking_test.jsonl` 和 `C:\training data\golf-ime-data\manifests\plan1_ranking_data_manifest.json` 存在。
- 未启动正式训练；交接命令仍为 `python training\plan1_reranker.py train`。

## 2026-06-01 Plan1 降档训练配置

### 当前有效状态

- 由于短时 GPU 前后向测速后出现整机级异常重启风险，Plan1 默认 `per_device_batch` 从 4 降为 3；`grad_accum=8`、encoder、fp16、gradient checkpointing、epoch 和验证策略保持不变。
- 后续 agent 的默认交接命令改为 `python training\plan1_reranker.py train --per-device-batch 3`；如果仍不稳定，再降到 `--per-device-batch 2`，不要直接降到 1。
- 未再次运行 GPU 压测；时间估计基于已完成的 `batch=4` 短测吞吐和当前 train split 精确规模。

### 训练时间重新估计

- 当前 train split：217,211 条样本，约 1,651,376 个 candidate pair。
- `batch=3, grad_accum=8`：每 epoch 约 72,404 个 micro-batch、9,051 个 optimizer step，有效 batch 为 24 条样本。
- 基于已测 `batch=4` 约 0.548 秒/micro-batch、约 53.2 candidate pair/s，`batch=3` 预计每个完整训练 epoch 约 8.9 到 9.9 小时。
- 至少 3 个 epoch 预计约 26.6 到 29.7 小时；若跑满 20 个 epoch，预计约 178 到 198 小时。验证和最终 test 会额外增加少量时间，最终以训练日志 `elapsed_seconds` 为准。

## 2026-06-04 Context reranker v2 异常高分审计闭环

### 当前有效状态

- 已合入 `docs/training_data_contract.md`，补充 0.995 异常高分审计口径、`v2-clean` 与 `production hybrid` 的边界、`context_after` 仅用于编辑/纠错场景的约束。
- 已新增 `training/context_reranker_v2.py` 作为 clean context 实验台；默认 `--context-mode online` 只使用 `context_before`，不默认使用未来下文。
- v2 审计目标从“模型结构是否干净”扩展为“用数字拆解高分来源”：候选原序、static rank、词频、记忆 baseline、split 泄漏、候选生成器偏置、困难负例和产品误伤率。
- 旧 `training/plan1_reranker.py` 未迁移、未替换；本次只新增 v2 审计/评估入口和训练数据合同，不把 v2 声明为生产模型。

### 本次修改

- `training/context_reranker_v2.py`
  - 新增 `audit-splits` 子命令，输出物理行数、有效/丢弃样本、candidate/target/source/domain/license 分布。
  - 新增固定 baseline：`candidates[0]`、`static_rank`、`frequency`、`random`、recall@10/30/100、train-to-val/test 记忆 baseline。
  - 新增 split 泄漏审计：完整样本、no-context 样本、`raw_input + target`、`context_suffix32 + raw_input + target`、`candidates_set + target`、`source_doc_key`、上下文窗口和近重复签名。
  - 新增候选生成器审计：target 位置、target 唯一可匹配风险、负例来源/匹配类型、困难负例比例、高频冲突比例。
  - 新增 eval/train sanity 开关：`--context-mode online|edit|none`、`--context-perturb none|shuffle|empty`、`--candidate-order original|shuffle`、`--label-mode normal|random`。
  - eval 输出 primary、online、edit、no-context、shuffled-context、shuffled-candidates、dummy-first/static/frequency/random 对照，以及 keep/fix/harm 产品指标。
  - JSONL 读取改为 `utf-8-sig`，兼容 Windows 工具写出的 UTF-8 BOM。
  - transformers 训练依赖改为 train/eval 时延迟导入，audit/test 不再因导入 v2 自动加载大模型依赖。
- `tests/test_context_reranker_v2.py`
  - 覆盖 context mode、baseline、记忆 baseline、split overlap、candidate shuffle、困难负例、推荐字段 `candidate_meta` 和产品误伤指标。
- `README.md`、`data/README.md`
  - 增加 v2 审计入口、常用命令和异常高分解释口径。

### 最终验证记录

- `python -m py_compile training\context_reranker_v2.py`：通过。
- `python -m pytest tests\test_context_reranker_v2.py -q`：8 passed。
- `python training\context_reranker_v2.py audit-splits --train <tmp>\train.jsonl --val <tmp>\val.jsonl --test <tmp>\test.jsonl --report <tmp>\audit.md`：通过；smoke 报告 kept samples=3，并正确触发 `raw_input_target`、`context_suffix32_raw_input_target` 与相关 red-line。
- import smoke：`InputMethodConfig`、`InputMethodEngine`、`PinyinCandidateGenerator`、`FrequencyReranker`、`GuiEditor`、`GuiCandidateWindow` 均可导入。
- `python -m pytest -q`：74 passed。
- `python scripts\evaluate_candidates.py`：通过；total Top-1 94.1%，Top-3/Top-5/召回覆盖率 100.0%。
- `python scripts\benchmark_latency.py`：通过；P99 约 0.06ms。
- `git diff --check`：通过；仅显示当前工作区已有 LF/CRLF 转换 warning。
- 未运行真实 checkpoint 的 `eval --checkpoint`，因为当前任务是审计接口与合同落地，没有提供或训练新的 v2 checkpoint。

## 2026-06-06 Context reranker v2 新语料训练准备闭环

### 当前有效状态

- context reranker v2 第一轮训练准备优先使用 `clean_dataset_v3\left_context_only` 下的新语料切分：`train_new_corpus.jsonl`、`val_new_corpus_v2.jsonl`、`test_new_corpus_v2.jsonl`。
- 旧 `train.jsonl` 暂不作为主训练集；原因是旧审计报告存在大量 CJK Ext-A/Ext-B-F 生僻字候选分布污染风险。
- v2 仍是 clean context 实验台，不接入输入法 UI，不替换 `ModelReranker`，也不把 smoke checkpoint 描述为可用 AI 排词能力。

### 本次修改

- `requirements-train.txt`
  - 明确保留训练依赖和运行时基础依赖分离，补齐 `torch`、`transformers`、`sentencepiece`、`rich`。
- `scripts/run_context_v2_local_audit.bat`
  - 新增 Windows 本地审计入口，调用同目录 PowerShell helper，避免 `cmd` 直接解析中文 Google Drive 路径导致乱码。
- `scripts/run_context_v2_local_audit.ps1`
  - 调用 `python training\context_reranker_v2.py audit-splits`，固定读取新 train/val/test corpus，并输出 `reports\context_v2_new_corpus_audit.md`。
- `notebooks/colab_context_reranker_v2.ipynb`
  - 新增 Colab notebook，覆盖 mount Google Drive、clone GitHub、checkout 当前分支、安装 `requirements-train.txt`、复制 Drive 数据、audit、smoke train、eval、离线 predict 和保存产物。
- `scripts/predict_context_reranker_v2.py`
  - 新增离线 checkpoint 推理入口，只接收 `context_before`、`composing` 和候选列表，输出排序候选和分数；不读取或拼接 `target`、`target_pinyin_full`、`target_pinyin_abbr`。
- `tests/test_context_reranker_v2.py`
  - 增加 predict 脚本样本构造与排序流程测试。
  - 增加 `audit-splits` 小样本 fixture 测试。
- `README.md`
  - 增加新 corpus 路径、本地 audit bat、Colab notebook 和离线 predict 命令说明。

### 最终验证记录

- `python -m py_compile training\context_reranker_v2.py scripts\predict_context_reranker_v2.py`：通过。
- `python -m unittest tests/test_context_reranker_v2.py`：10 tests OK。
- `scripts\run_context_v2_local_audit.bat`：通过；生成 `reports\context_v2_new_corpus_audit.md`。
- 本地新语料 audit：kept samples 共 146,259；train/val/test 为 116,572 / 15,686 / 14,001。
- 本地新语料 audit 红线：`train-val raw_input+target overlap 0.7961 > 0.30`，`train-test raw_input+target overlap 0.8002 > 0.30`。该红线未被吞掉，正式训练前需要决定是否接受该风险或回到数据清洗/切分。
- smoke train：使用 `.smoke_data\context_v2_new_corpus_smoke\train_smoke.jsonl`、`val_smoke.jsonl` 和 `hf-internal-testing/tiny-random-bert` 在 CPU 跑通 1 epoch；生成 `.smoke_data\context_v2_new_corpus_smoke\checkpoint`。
- smoke eval：通过；输出 `.smoke_data\context_v2_new_corpus_smoke\eval_smoke.json`。
- 离线 predict：通过；`scripts\predict_context_reranker_v2.py` 可加载 smoke checkpoint 并输出候选排序。
- import smoke：`InputMethodConfig`、`InputMethodEngine`、`PinyinCandidateGenerator`、`FrequencyReranker`、`GuiEditor`、`GuiCandidateWindow` 均可导入。
- `python scripts\evaluate_candidates.py`：通过。
- `python scripts\benchmark_latency.py`：通过。
- `git diff --check`：通过；仅显示当前工作区已有 LF/CRLF 转换 warning。
- Computer Use Windows helper 当前不可用，返回 `Computer Use native pipe path is unavailable`；因此未能通过 Computer Use 实际打开 Colab。Colab notebook 已做 JSON 解析验证，本地 smoke 链路已跑通。
