# golf 项目目标、可用性差距与后续开发约束

本文档用于约束后续 Gemini / Codex / Claude / Opus 等 agent 对 golf 的理解和改动边界。任何涉及输入法原型、候选生成、排序模型、词库、用户记忆、日语模式、GUI、评估或性能的修改，都必须先阅读本文档。

本文档不是产品宣传材料，而是当前工程状态、目标差距和后续优先级的约束文档。后续任何实现、文档、汇报和验收都必须以真实代码、真实命令和真实验证结果为准。

## 一、项目真实目标

golf 的最终目标是构建一个由大语言模型参与选词/排词的 AI 输入法。大语言模型不一定负责候选生成的全部流程，但必须能根据上下文、当前输入、候选列表、用户偏好和历史选择，对候选词、候选短语、句子补全进行排序或重排。

当前阶段不能依赖大语言模型才能可用。当前阶段目标是先构建一个无需大语言模型也能使用、效果不差的输入法底座。

这个底座应承担四个角色：

1. 用户当前可以使用的基础输入法。
2. 大语言模型排词接入前的传统 baseline。
3. 大语言模型失效、关闭或延迟过高时的 fallback。
4. 后续选词模型研究的稳定工程宿主。

这里的方向不是“普通传统输入法替代 AI 输入法”，而是“先有可靠传统底座，再接入大语言模型选词”。如果传统底座不可靠，后续 LLM 排词就没有稳定候选、对照组、回退路径和工程宿主。

## 二、当前总评

根据当前代码状态，项目已经有输入法 Engine 原型、内置编辑器 Demo、候选窗口、少量候选召回、词频排序和模型接口桩。

当前还不能称为：

- 可日用输入法。
- 效果不差的输入法。
- 已完成的大语言模型选词输入法。
- 已具备真实用户记忆能力的输入法。
- 已具备真实日语输入能力的输入法。

按用户目标评分：

- 最终目标“大语言模型选词输入法”：当前约 5/100。
- 当前阶段“不用大模型也能用且不差”：当前约 15/100 ~ 25/100。
- 真实中文词库：约 5/100。
- 拼音候选生成：约 10/100。
- 传统排序 baseline：约 20/100。
- 用户常用词记忆和权重更新：约 0/100 ~ 5/100。
- 日语模式：约 0/100。
- 真实模型排词：约 0/100 ~ 5/100。
- 输入体验：约 20/100。
- 性能验证：约 10/100。
- 测试与评估体系：约 25/100。

以上评分不是否定已有骨架，而是防止后续 agent 把 Demo 当成可用产品。当前骨架有价值，但它仍处在原型阶段，距离“无需大模型也能用且效果不差”的底座还有明显工程差距。

## 三、阶段路线

项目后续分为两个大阶段。

阶段 A：无需大模型也能用的输入法底座。

目标：

- 有真实词库。
- 有拼音候选生成。
- 有传统排序 baseline。
- 有用户常用词记忆。
- 有用户词权重更新。
- 有基础 GUI 输入体验。
- 有按钮切换日语模式。
- 有评估和性能基准。

阶段 B：大语言模型选词/排词。

目标：

- 大语言模型接收上下文、composing、候选列表和用户偏好特征。
- 大语言模型输出候选排序分数或重排结果。
- 大语言模型排序必须优于传统 baseline。
- 大语言模型不可用时自动退回传统 baseline。
- 大语言模型接入必须有真实评估和延迟报告。
- 禁止 STUB 冒充模型。

阶段 A 未达到最低可用前，不应优先做 UI 美化、宣传按钮或无法验证的 AI 能力描述。阶段 B 必须建立在阶段 A 的候选生成、词库、用户记忆、评估体系和 fallback 之上。

## 四、当前阶段的最低可用目标

当前阶段不是要做完最终 AI，而是要达到：

“不启用大语言模型，也能作为一个基础输入法使用，而且效果不差。”

最低要求包括：

1. 真实词库。
   - 不再依赖几十个硬编码词条。
   - 支持外部词库加载。
   - 支持至少 1 万词 smoke，后续推进到 10 万级常用词/短语。
   - 词库字段至少包含 `word`、`pinyin`、`short_pinyin`、`freq`、`source`。
   - 当前 `RAW_WORDS` 只能作为 fallback 和测试样例。

2. 拼音候选生成。
   - 支持全拼。
   - 支持简拼。
   - 支持前缀输入。
   - 支持连续拼音切分。
   - 支持词和短语候选。
   - 输入类似 `woxiangyao` 时，应能产生合理候选路径，例如“我 想 要”“我 想要”这类结果。
   - 不允许只靠 `startswith` 冒充成熟候选生成。

3. 传统排序 baseline。
   - 不启用大语言模型时，也要有可解释排序。
   - baseline 至少考虑：词频、完整匹配优先、短语优先、上下文共现、用户最近选择、用户词频、切分惩罚、候选长度惩罚。

4. 用户常用词记忆和权重更新。
   - 必须支持用户选择某个候选后，提高该候选在相同或相近输入下的权重。
   - 必须支持常用词权重持久化。
   - 必须支持权重衰减或至少避免无限增长。
   - 必须支持用户词库或用户频率文件的加载和保存。
   - 必须保护隐私：默认不记录完整明文长句日志；如记录用户词频，应尽量只记录词条、拼音键、计数、最近使用时间等最小必要字段。
   - 必须有清空用户学习数据的接口或文件说明。
   - 用户记忆必须影响排序结果，并有测试证明。

建议用户词频结构为 `user_memory.json` 或 `user_lexicon.jsonl`。

字段示例：

- `word`：候选词。
- `input_key`：对应拼音或输入键。
- `weight`：用户权重。
- `count`：选择次数。
- `last_used_at`：最近使用时间戳。
- `source`：`user_selected` / `user_added`。
- `enabled`：是否启用。

排序中应加入：

```text
user_score = f(count, weight, recency)
```

5. 日语模式。
   - 当前阶段暂时只要求通过 GUI 按钮切换日语。
   - 不要求键盘快捷键切换日语。
   - 必须在模式枚举或配置中预留 `japanese` / `ja` 模式。
   - GUI 中应有明确按钮，例如“切换中文/英文/日语”或独立“日语模式”按钮。
   - 切换日语模式后，Engine 状态、候选生成器、候选窗口显示必须一致。
   - 如果暂时没有完整日语输入能力，必须诚实显示“日语模式原型 / 日语候选未完整实现”。
   - 不允许只加按钮却在文档里声称已完成日语输入法。

日语模式第一阶段可以是最小实现：

- 有模式状态。
- 有按钮切换。
- 有独立 `JapaneseCandidateGenerator` 接口。
- 可以先支持少量 romaji -> kana / 常用假名候选，用于证明链路。
- 必须明确这只是日语模式骨架，不是完整日语输入法。

## 五、最终大语言模型选词目标

大语言模型不是用来替代所有工程基础。大语言模型只负责选词/排词时，仍然需要候选生成、词库、用户记忆、评估体系和 fallback。

LLM Reranker 的理想输入：

- `context_before`：光标前上下文。
- `context_after`：光标后上下文，可选。
- `composing`：当前输入串。
- `mode`：`pinyin` / `english` / `japanese`。
- `candidates`：候选列表。
- `candidate_features`：候选词频、来源、匹配方式、用户权重、最近使用等。
- `user_preferences`：可选，本地隐私特征。
- `domain_context`：可选，当前应用或领域。

LLM Reranker 的输出：

- `candidate_scores`。
- `reranked_candidates`。
- `optional_debug_reason`，不记录隐私原文时可关闭。

必须要求：

- LLM 排词必须和传统 baseline 对比。
- LLM 排词必须有 Top-1、Top-3、排序损失、延迟指标。
- LLM 排词失败时必须 fallback 到传统排序。
- LLM 排词不能阻塞输入体验。
- 没有真实模型推理时，不能显示“AI 排词已启用”。

## 六、当前代码中必须警惕的问题

根据当前代码状态，后续开发必须警惕以下问题：

1. GUI 当前只是内置编辑器 Demo，不能说是正式输入法。
2. `PinyinCandidateGenerator` 当前词库太小。
3. `EnglishCandidateGenerator` 只是英文前缀补全，不能代表中文输入体验。
4. `ModelReranker` 目前只是 STUB，不能说是 AI 模型。
5. `FrequencyReranker` 只是按分数排序，baseline 还很弱。
6. 当前没有用户常用词记忆和权重更新。
7. 当前没有日语模式。
8. 当前没有真实规模性能测试。
9. 当前测试不能证明输入法可用。
10. GUI 不应直接调用 `engine.__init__()` 或直接改 Engine 私有状态。

这些问题不要求在本文档变更中修复，但必须在后续实现和汇报中如实承认，并按优先级逐步处理。

## 七、后续优先级

当前阶段优先级：

真实词库
> 拼音切分
> 候选召回
> 传统排序 baseline
> 用户常用词记忆和权重更新
> 日语模式按钮与日语 generator 骨架
> 评估体系
> 性能 benchmark
> 真实大语言模型排词接口
> GUI 美化

特别强调：

- 不要继续优先美化 UI。
- 不要继续增加宣传性按钮。
- 不要继续写无法验证的“AI 已实现”。
- 不要把日语按钮等同于完整日语输入法。
- 不要把用户记忆写成只在内存里临时加分，必须有持久化和测试。

## 八、验收标准

Milestone A：诚实原型。

- README 和 AGENTS 写清项目仍是原型。
- 模型 STUB 不被宣传成 AI。
- GUI 被描述为内置编辑器 Demo。
- 当前测试通过。

Milestone B：无需大模型的可用底座 v0。

- 支持外部中文词库。
- 支持至少 1 万词 smoke。
- 支持拼音切分初版。
- 支持传统排序 baseline。
- 支持用户常用词权重更新和持久化。
- 支持清空用户学习数据。
- GUI 有按钮切换日语模式。
- 日语模式有独立 generator 骨架。
- 有候选质量评估脚本。
- 有 P50 / P95 / P99 延迟 benchmark。

Milestone C：无需大模型的可用底座 v1。

- 支持 10 万级词库。
- Top-1 / Top-3 指标可复现。
- 用户记忆能显著影响排序。
- 常见输入路径体验合理。
- 日语模式至少支持最小 romaji/kana 候选链路。
- 传统 baseline 可作为 LLM 对照组。

Milestone D：真实大语言模型选词。

- 真实模型加载。
- 真实模型推理。
- 模型输出影响排序。
- 指标优于传统 baseline。
- 延迟可接受。
- 失败自动 fallback。

Milestone E：更接近日用。

- 输入体验稳定。
- 文档真实。
- 有安装、启动、故障排查。
- 有回归测试和性能报告。

## 九、自动化代理禁止事项

后续任何 agent 都不得做以下事情：

1. 把内置编辑器 Demo 描述成正式输入法。
2. 把模型桩描述成 AI 模型。
3. 把硬编码规则描述成真实模型推理。
4. 把几十个词条描述成词库完善。
5. 把没有持久化的临时加分描述成用户记忆系统。
6. 把日语按钮描述成完整日语输入法。
7. 把 `py_compile` 描述成完整验证。
8. 用“完美”“极其顺畅”“黑科技”等词包装未充分验证的功能。
9. 继续堆 UI 来掩盖候选质量不足。
10. 跳过评估指标直接宣称体验变好。
11. 直接调用对象 `__init__()` 来重置状态。
12. GUI 直接修改 Engine 内部状态或调用私有方法绕过公开接口。
13. 吞异常后继续显示成功。
14. 把历史比赛结果描述成当前输入法能力。
15. 把未完成路线写成已完成状态。

## 十、推荐给后续 agent 的任务模板

请不要继续美化 UI，也不要宣传 AI 能力。当前任务是把 golf 推进到“不需要大语言模型也能用，而且效果不差”的输入法底座。

必须完成：

1. 新增外部中文词库格式和加载器。
2. 当前 `RAW_WORDS` 仅保留为 fallback。
3. 实现拼音音节表和连续拼音切分初版。
4. 实现候选路径召回，支持全拼、简拼、前缀。
5. 实现传统排序 baseline。
6. 实现用户常用词记忆和权重更新，必须持久化。
7. 实现清空用户学习数据的接口或文档。
8. GUI 增加按钮切换日语模式，不要求键盘快捷键。
9. 新增 `JapaneseCandidateGenerator` 骨架。
10. 新增候选覆盖率评估脚本。
11. 新增性能 benchmark，输出 P50 / P95 / P99。
12. 更新文档，明确当前仍未接入真实大语言模型排词。

- 不把日语按钮说成完整日语输入法。
- 不把临时内存加分说成用户词库。

## 十一、完整输入法能力地图

一个完整的工业级 AI 输入法系统应该包含以下 12 类能力：

1. **系统集成**
   - Windows/macOS/Linux 输入法框架接入
   - 全局可用，不只是在自带编辑器里
   - 应用间切换稳定
   - 光标位置、候选窗定位、焦点恢复
   - 安装、卸载、开机启动、托盘状态

2. **输入状态机**
   - composing 缓冲区
   - 光标移动、插入、删除
   - 空格选首词
   - 数字选词
   - 翻页
   - 回车上屏原文
   - Esc 取消
   - 中英文/符号/全半角切换

3. **拼音解析**
   - 全拼
   - 简拼
   - 前缀输入
   - 连续拼音切分
   - 多切分路径
   - 模糊音
   - 纠错拼音
   - 声母韵母边界识别
   - 实时增量解析

4. **候选召回**
   - 单字候选
   - 词语候选
   - 短语候选
   - 句子补全
   - 英文补全
   - 符号候选
   - emoji 候选
   - 时间日期候选
   - 用户自定义短语
   - 多语言候选

5. **词库系统**
   - 内置基础词库
   - 外部词库导入
   - 用户词库
   - 领域词库
   - 词频
   - 拼音、简拼、别名
   - 词库版本管理
   - 词库校验和回滚
   - 大词库加载性能优化

6. **排序 baseline**
   - 静态词频
   - 完整匹配优先
   - 短语优先
   - 上下文共现
   - 最近使用
   - 用户词频
   - 切分路径惩罚
   - 候选长度控制
   - 应用/场景偏置
   - 可解释打分

7. **用户学习**
   - 选词后提权
   - 用户词持久化
   - 最近使用衰减
   - 频率上限或归一化
   - 自造词学习
   - 删除用户词
   - 清空学习数据
   - 隐私最小化存储
   - 跨设备同步，后期可选

8. **候选窗口与交互**
   - 跟随光标
   - 分页展示
   - 高亮首选
   - 候选编号
   - 鼠标选择
   - 键盘选择
   - 横排/竖排
   - 主题与字号
   - 多屏 DPI 适配

9. **AI/LLM 排词**
   - 接收上下文、composing、候选列表
   - 输出候选分数或重排结果
   - 不阻塞输入
   - 超时 fallback
   - 和传统 baseline 对比
   - Top-1/Top-3/排序损失评估
   - 隐私保护
   - 可关闭
   - 不用 STUB 冒充真实模型

10. **性能与稳定性**
    - P50/P95/P99 延迟
    - 大词库加载时间
    - 内存占用
    - 冷启动时间
    - 长时间输入稳定性
    - 异常恢复
    - 后台资源占用
    - 无候选、坏词库、坏配置 fallback

11. **评估体系**
    - 候选覆盖率
    - Top-1 命中率
    - Top-K 命中率
    - 排序损失
    - 真实输入路径 smoke
    - 用户记忆效果测试
    - 性能 benchmark
    - 回归测试集
    - 文档化验证报告

12. **配置与运维**
    - 配置文件
    - GUI 设置页
    - 词库路径配置
    - 用户记忆路径配置
    - 日志开关
    - 隐私开关
    - 导入/导出用户词
    - 故障排查命令
    - 安全更新和版本迁移

> [!WARNING]
> **本轮不是完成完整输入法的 12 层能力；本轮只交付“勉强可用 v0”**。我们仅在自带的记事本 GUI 与 CLI 模拟器中提供这 12 层能力的最小可用子集以打通输入到选词上屏的闭环。不得因为完整能力范围很大而停止实现。

## 最终提交态验证强约束


1. 所有验收均以最终工作区 / 最终提交态为准。
2. 中途测试结果在后续任何文件写入后立即失效。
3. 任何文件批量改写、格式化、行尾清理之后，必须重新跑完整最终验证。
4. 禁止读写同一路径的 Python one-liner。
5. 禁止用 `open(path, "w")` 和 `open(path, "r")` 在同一表达式或同一列表推导中处理同一路径。
6. `py_compile` 只能证明语法可解析，不能证明模块包含所需类或功能。
7. 提交前必须验证关键类可导入：
   - InputMethodConfig
   - InputMethodEngine
   - PinyinCandidateGenerator
   - FrequencyReranker
   - GuiEditor
   - GuiCandidateWindow
8. 提交前必须运行：
   - import smoke
   - pytest
   - evaluate_candidates.py
   - benchmark_latency.py
   - git diff --check
9. 修改记录只能写最终验证结果，不得写中途结果。
10. 如果最终态失败，必须写”未完成”，不得写”完成”。

## 十四、2026-05-30 补齐：词库方案 B & 10 万 smoke

### 新增工具

- `scripts/generate_synthetic_lexicon.py` — 生成 10 万级合成拼音词库用于工业级压测
- `scripts/benchmark_lexicon.py` — 词库加载/查询/内存压测 (P50/P95/P99)
- `data/lexicon/README.md` — 词库目录说明和操作流程

### 10 万 smoke 结果 (方案 B)

```
python scripts/generate_synthetic_lexicon.py --count 100000 --output .smoke_data/lexicon_100k.jsonl
→ 生成 100,000 条, 文件大小 10.4 MB, 耗时 0.52s

python scripts/import_lexicon.py --input .smoke_data/lexicon_100k.jsonl --output .smoke_data/imported_lexicon.jsonl
→ 去重后 99,449 条

python scripts/benchmark_lexicon.py --dict-path .smoke_data/imported_lexicon.jsonl
→ 加载耗时: 0.231s | P95 查询: 22.06ms | 内存: 46.5MB
→ ✅ 10 万级词库加载达标
```

### 词库方案结论

采用**方案 B**：无法合法提交真实大词库时，提供完整的词库生成、导入和压测工具。
`data/lexicon/dict.jsonl` (~435 条) 是手工整理的无版权常用词示例，**不是工业级词库**。
工具链可用于生成、导入和压测任意规模的词库。
